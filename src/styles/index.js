import { StyleSheet } from 'aphrodite';

const styles = StyleSheet.create({
  appContainer: {
    padding: '.8em',
  },
  menuItem: {
    textDecoration: 'none'
  },
  activeMenuItem: {
    color: 'red',
    fontWeight: 'bold',
  }
});

export default styles
